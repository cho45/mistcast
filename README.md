# Mistcast

Mistcastは、ブラウザ上で動作する**音響データ通信（Data-over-Sound）ウェブアプリケーション**です。
スピーカーとマイクを使用し、音響空間を介したデータ伝送を実装しています。

https://github.com/user-attachments/assets/9e2b40ab-bfc8-4fa5-8e5b-f9ce8e6f0eb9

## 特徴

- **ハイブリッド変調アーキテクチャ**: 安定性重視の DSSS (Slow) モードと、高速・高密度な M-ARY (Fast) モードを選択可能。
- **ファウンテンコード（消失訂正符号）によるデータ復元**: RLNC（ランダム線形ネットワークコーディング）を採用。送信側は符号化パケットを継続的に射出し、受信側は任意の順序で必要な数のパケットを受信した段階で元のデータを復元可能です。
- **ストリームの任意位置からの復号**: 各バーストが独立した同期情報（プリアンブル）を持つ設計となっており、送信済みのストリームのどのタイミングから受信を開始してもデコード可能です。
- **ブラウザ標準機能のみを利用**: Web Audio API および WebAssembly (SIMD) を使用し、プラグインなしで現代的なブラウザ上で動作します。

## 技術仕様

### 1. 物理レイヤー (物理変調)

Mistcast は、用途に応じて2つの物理層実装を提供します。

| 特徴 | DSSS (Slow) | M-ARY (Fast) |
| :--- | :--- | :--- |
| **変調方式** | DSSS + DQPSK | 16-ary Orthogonal + DQPSK |
| **伝送効率** | 2 bits / symbol | **6 bits / symbol** |
| **インターリーバ** | 16×22=352 bits | **29×12=348 bits (効率100%)** |
| **拡散率 (SF)** | 15 (M系列) | 16 (Walsh-Hadamard) |
| **チップレート** | 8,000 cps | 8,000 cps |
| **キャリア周波数** | 15,000 Hz | 15,000 Hz |
| **実効速度 (Goodput)** | **~ 348 bps** (1 pkt/burst) | **~ 1114 bps** (3 pkts/burst) |
| **マルチパス対策** | N/A | **FDE + 自動パス選択** |

- **共通基盤**:
    - **対称リサンプリング**: 入出力サンプリングレートを内部処理レート（$3 \times R_c$）へ変換し、タイミング累積誤差を抑制。
    - **RRC (Root-Raised Cosine) フィルタ**: ロールオフ係数 0.30 による波形整形。
    - **軟判定復調**: Max-Log-MAP 近似による LLR (対数尤度比) 算出と Viterbi 復号の統合。

## 同期頻度と実効速度

Mistcastは、**「頻繁に同期（プリアンブル）を送り直す」**設計を採用しています。デフォルトでは、約 370ms ごとに同期信号が挿入されます。

### 頻繁な同期のメリット・デメリット

| メリット | デメリット |
| :--- | :--- |
| **即時復帰**: ノイズや遮蔽で同期が外れても、次のバースト（数百ms後）で即座に復帰可能。 | **オーバーヘッド**: 同期信号（33.75ms）が占める時間の分、純粋な伝送速度が低下する。 |
| **ファウンテンコードとの相性**: どの位置から受信しても、最初の同期さえ掴めれば即座にデータ断片を得られる。 | **音響的なノイズ**: 同期信号（プリアンブル）が頻繁に鳴るため、聴感上の「通信音」が目立つ。 |
| **クロックドリフト耐性**: 送受信間のサンプリングレート微差によるタイミングズレが蓄積する前にリセットされる。 | |

### モード別の伝送効率（同期周期を一定にした場合）

同期の堅牢性（やり直しが効く頻度）を維持するため、同期周期を **約 370ms** に揃えた場合の比較です。

| モード | バースト構成 | 同期周期 | 伝送量 / バースト | 同期比率 |
| :--- | :--- | :--- | :--- | :--- |
| **DSSS (Slow)** | 1 パケット | **363.75 ms** | 128 bits | 9.3% |
| **M-ARY (Fast)** | 3 パケット | **387.75 ms** | **384 bits** |  **8.7%** |

M-ARY (Fast) モードでは、1シンボルあたりの情報密度が 3倍（6 bits/symbol）であるため、**「同期のやり直しチャンス」を減らすことなく、データ伝送量だけを約3倍に増やす**ことが可能です。また、同期信号が占める時間的割合（オーバーヘッド）も M-ARY (Fast) の方が僅かに低くなり、より効率的な通信を実現しています。

### 変調方式のトレードオフ

速度（Fast）と引き換えに発生する物理的なトレードオフは以下の通りです。

| 特性 | DSSS (Slow) | M-ARY (Fast) | 理由 |
| :--- | :--- | :--- | :--- |
| **ノイズ耐性 (SNR)** | **高い** | 中程度 | M-ARYは16チップで6ビットを送るため情報密度が3倍高いが、その分1ビットあたりのエネルギーが分散され、DSSSに比べてノイズ耐性が相対的に低下する。 |
| **マルチパス耐性** | **高い** | 中高 | ウォルシュ・アダマール系列は直交性を維持するために正確なタイミングが必要。M-AryのみFDEと自動パス選択。 |
| **計算負荷 (CPU)** | **低い** | 高い | 受信時に16個の相関器を並列稼働させる必要がある（DSSSは1個）。 |
| **最適環境** | 極低SNR・屋外 | 静かな室内・近距離 | 速度よりも到達性を優先する場合は DSSS、スループット優先なら M-ARY。 |

## フレーム構造とパケット構造

Mistcastは、物理層（変調フレーム）とデータリンク層（LT符号パケット）の2層構造でデータを伝送します。

### 変調フレーム (Physical Layer Frame)
1回の同期（プリアンブル）で送信される単位です。

```text
|<--- Preamble --->|<- Sync ->|<---------- Data Symbols (Payload) ---------->|
+------------------+----------+----------------------------------------------+
| PN Sequence x 2  | 8-16 bit | DQPSK Symbols (Packet 1, [Packet 2, ...])    |
| (M-Seq or ZC)    | (DBPSK)  | (DSSS or M-ARY Spreading)                    |
+------------------+----------+----------------------------------------------+
^                  ^          ^
|                  |          +-- Payload開始 (基準位相)
|                  +-- フレーム境界確定
+-- タイミング同期 / AGC
```

- **Preamble系列**: DSSS モードは **M系列** (SF=15)、M-ARY モードは **Zadoff-Chu系列** (SF=71) を使用。
- **Sync Word長**: DSSS モードは 16ビット、M-ARY モードは 8ビットを使用。

### パケットフォーマット (Link Layer Packet)
1つのパケットは 21 バイトで構成され、消失訂正符号（ファウンテンコード）の最小単位となります。

```text
|<--- Header (3B) --->|<--- Payload (16B) --->|<-- CRC (2B) -->|
+----------+----------+-----------------------+----------------+
|  lt_k-1  |  lt_seq  |      Data Bytes       |    CRC-16      |
|  (8-bit) | (16-bit) |      (16 bytes)       |   (CCITT)      |
+----------+----------+-----------------------+----------------+
```

- **lt_k**: データを分割した際の総ソースブロック数 (1〜255)。
- **lt_seq**: ファウンテンコードのシーケンス番号。
- **データサイズ制限**: `lt_k` が 8ビット（最大255）であるため、一度に送信可能な最大データサイズは **$255 \times 16 = 4,080$ バイト**に制限されます。

### パケットの物理層へのマッピング

DSSSモードとM-ARYモードで、パケットの物理シンボルへのマッピングが異なります。

#### DSSS (Slow) モード
```
パケット (21B) → FECエンコード (174→348 bits) → インターリーバ (352 bits)
                                     ↓
                    DQPSK変調 (2 bits/symbol) → 176 symbols
                    ↓
                    DSSS拡散 (SF=15) → 2,640 chips
```

#### M-ARY (Fast) モード
```
パケット (21B) → FECエンコード (174→348 bits) → インターリーバ (348 bits)
                                     ↓
                    MaryDQPSK変調 (6 bits/symbol) → 58 data symbols
                    ↓
                    DSSS拡散 (SF=16) → 928 chips
```

**主な違い**:
- **インターリーバサイズ**: DSSSは352ビット（4ビットパディング）、M-ARYは348ビット（パディングなし）
- **変調効率**: DSSSは2 bits/symbol、M-ARYは6 bits/symbol（3倍の密度）
- **拡散率**: DSSSはSF=15、M-ARYはSF=16

## 同期捕捉 (Synchronization)

安定した同期を実現するため、フレームのセクションごとに変調を切り替えます。

### プリアンブル（Preamble）
タイミング同期とキャリア同期に使用されます。

| モード | 系列 | 拡散率 | パターン | 変調方式 | 特徴 |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **DSSS (Slow)** | M系列 | SF=15 | [M, -M] | DBPSK | シンプルで高SNR環境で強力 |
| **M-ARY (Fast)** | Zadoff-Chu | SF=71 | [ZC, -ZC] | Walsh[0]+DBPSK | 低自己相関でマルチパス耐性が高い |

- **最終シンボル反転**: 両モードとも最後のシンボルを反転させ、同期の位相曖昧さを排除
- **繰り返し回数**: `preamble_repeat` パラメータで調整可能（デフォルト2回）

### Sync Word
フレーム境界の確定とリファレンス位相の確立に使用されます。

| モード | Sync Word長 | 変調方式 |
| :--- | :--- | :--- |
| **DSSS (Slow)** | 16ビット | DBPSK |
| **M-ARY (Fast)** | 8ビット | DBPSK |

- **固定パターン**: `0xDEAD_BEEF` (下位ビットから使用)
- **機能**: フレーム境界の確定とリファレンス位相の確立

### Handoff
- Sync Word 終了時の位相を Payload 領域の初期位相として引き継ぎ

### 3. 誤り訂正と消失訂正

- **FEC**: 畳み込み符号（**拘束長K=7**, 符号化率**R=1/2**, NASA Voyager標準）とソフト判定 Viterbi 復号。
  - 生成多項式: G1=0o171 (121), G2=0o133 (91)
  - 状態数: 2^(K-1) = 64状態
- **CRC-16**: パケット単位の誤り検出（CRC-CCITT）。
- **インターリーバ**: バースト誤り分散のためのブロックインターリーバ
  - **DSSS**: 16×22=352ビット（FEC後348ビットから4ビット増加）
  - **M-ARY**: 29×12=348ビット（FECビット数と完全一致、効率100%）
- **RLNC (Random Linear Network Coding)**: GF(256) 上の消失訂正符号. パケットの欠落や順序入れ替わりに強い耐性を持ちます。

### 4. M-ARYモードの高度機能

#### FDE（周波数領域等化器）
マルチパスフェージング環境での信号品質を改善します。

- **MMSE等化**: 最小平均二乗誤差基準による等化
- **FFTベース処理**: オーバーラップ加算法で効率的な処理
- **CIR推定**: プリアンブルからチャネルインパルス応答を推定

#### FDE自動パス選択
チャネル状態に応じてFDE出力と生信号を自動的に切り替えます。

- **MSE比較**: FDE処理後と生信号のMSEを比較して良好な方を選択
- **適応処理**: 静かな環境では生信号、マルチパス環境ではFDEを自動選択

#### チャネル品質推定
受信信号の品質をリアルタイムで推定します。

- **SNR推定**: dB単位での信号対雑音比推定
- **CFO推定**: キャリア周波数オフセット推定
- **ノイズ分散推定**: チャネルノイズの分散推定

## ディレクトリ構造

```text
.
├── src/                # Webフロントエンド (Vue 3 / TypeScript)
│   ├── components/     # UIコンポーネント (Sender/Receiver)
│   ├── worker.ts       # DSP処理を実行する Web Worker
│   └── audio-processors.ts # Web Audio API と WASM のブリッジ
├── dsp/                # DSPコアエンジン (Rust)
│   ├── src/
│   │   ├── lib.rs              # WASMパブリックAPI
│   │   ├── common/             # 数学的プリミティブ (Walsh, M系列, RRC, NCO, Resample)
│   │   ├── dsss/               # DSSS (Slow) モード: DSSS+DQPSK 実装
│   │   ├── mary/               # M-ARY (Fast) モード: 16-ary+DQPSK 実装
│   │   ├── coding/             # 符号化層 (FEC, Fountain, Interleaver, Scrambler)
│   │   └── frame/              # データリンク層 (Packet Format)
│   └── tests/          # 通信シミュレーション・ノイズ耐性テスト
└── scripts/            # WASMビルド・評価用Pythonスクリプト
```

## 開発と評価

### クイックスタート
1. **依存関係のインストール**: `npm install`
2. **WASM コアのビルド**: `npm run build:wasm`
3. **開発サーバーの起動**: `npm run dev` (`http://localhost:5173`)

### テストとベンチマーク
- **DSP Core ユニットテスト**: `cd dsp && cargo test --release`

### 評価スクリプト

#### M-Ary

```
$ cargo run --release --bin dsss_e2e_eval -- \
        --phy mary \
        --mode sweep-awgn \
        --packets-per-frame 3 \
        --sweep-awgn '0.0,0.40,0.45,0.50,0.55,0.60,0.65,0.70' \
        --columns 'scenario,synced_frame_ratio,crc_pass_ratio,goodput_effective_bps,goodput_success_mean_bps' \
        --output table
```
| scenario                       | synced_frame_ratio | crc_pass_ratio  | goodput_effective_bps | goodput_success_mean_bps |
| :----------------------------- | :-------------- | :-------------- | :-------------- | :-------------- |
| sweep_awgn(sigma=0.000)        | 1.0000          | 1.0000          | 1001.5680       | 1113.8733       |
| sweep_awgn(sigma=0.400)        | 1.0000          | 0.9576          | 908.2015        | 1038.2769       |
| sweep_awgn(sigma=0.450)        | 1.0000          | 0.9106          | 840.2986        | 994.6066        |
| sweep_awgn(sigma=0.500)        | 1.0000          | 0.8495          | 772.3957        | 926.5545        |
| sweep_awgn(sigma=0.550)        | 1.0000          | 0.6822          | 619.6141        | 785.7986        |
| sweep_awgn(sigma=0.600)        | 1.0000          | 0.3685          | 331.0267        | 439.4499        |
| sweep_awgn(sigma=0.650)        | 1.0000          | 0.1280          | 110.3422        | 239.3120        |
| sweep_awgn(sigma=0.700)        | 1.0000          | 0.0264          | 16.9757         | 45.7726         |

#### DSSS

```
$ cargo run --release --bin dsss_e2e_eval -- \
        --phy dsss \
        --mode sweep-awgn \
        --packets-per-frame 1 \
        --sweep-awgn '0.0,0.40,0.45,0.50,0.55,0.60,0.65,0.70' \
        --columns 'scenario,synced_frame_ratio,crc_pass_ratio,goodput_effective_bps,goodput_success_mean_bps' \
        --output table
```

| scenario                       | synced_frame_ratio | crc_pass_ratio  | goodput_effective_bps | goodput_success_mean_bps |
| :----------------------------- | :-------------- | :-------------- | :-------------- | :-------------- |
| sweep_awgn(sigma=0.000)        | 4.4244          | 0.2116          | 339.5652        | 348.3165        |
| sweep_awgn(sigma=0.400)        | 11.2500         | 0.0708          | 288.6304        | 310.3037        |
| sweep_awgn(sigma=0.450)        | 9.2791          | 0.0896          | 297.1195        | 315.4940        |
| sweep_awgn(sigma=0.500)        | 8.9884          | 0.0925          | 297.1195        | 310.2292        |
| sweep_awgn(sigma=0.550)        | 11.3779         | 0.0703          | 271.6521        | 295.4549        |
| sweep_awgn(sigma=0.600)        | 11.0407         | 0.0700          | 280.1413        | 297.3710        |
| sweep_awgn(sigma=0.650)        | 9.8488          | 0.0797          | 280.1413        | 301.1815        |
| sweep_awgn(sigma=0.700)        | 13.8663         | 0.0482          | 237.6956        | 271.7278        |



## ライセンス

MIT License
